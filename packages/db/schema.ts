/**
 * @deprecated This file is a stale snapshot. The canonical database schema
 * lives in `apps/api/src/drizzle/schema.ts` which is kept in sync with
 * the migration files under `apps/api/migrations/`.
 *
 * If you need to share schema types across services, import from the API
 * package or extract the relevant types into this package.
 *
 * See: apps/api/src/drizzle/schema.ts
 */
export {};
